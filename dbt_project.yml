name: 'movielens'          # 👈 must NOT be "dbt"
version: '1.0.0'
profile: 'dbt'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  movielens:               # 👈 must match name above
    +materialized: view
    dim:
      +materialized: table
    fct:
      +materialized: table
